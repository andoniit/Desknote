/**
 * Supabase-generated types are ideal: `supabase gen types typescript --linked`
 * and paste the result here for full PostgREST inference.
 *
 * Until then, `any` keeps the client compatible with @supabase/supabase-js
 * strict `GenericSchema` checks (hand-written shapes often collapse to `never`).
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
